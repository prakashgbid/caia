# Scaffold template for @chiefaia/aiml-architect
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_AIML_ARCHITECT
title: "Typical work shape — aiml-architect"
description: |
  Package: @chiefaia/aiml-architect
  Survey-line: AI/ML Architect Agent — first of 12 domain-specialist architect agents (item 8.5 in master sequencing). Unifies scattered AI/ML decisions across CAIA into a coherent practice: model selection (Claude 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/aiml-architect/src/<feature>.ts
  - packages/aiml-architect/tests/<feature>.test.ts
  - packages/aiml-architect/README.md
success_criteria:
  output_file: reports/caia/aiml-architect-<feature>.md
  min_bytes: 1800
  grep_match: "Context|Decision|verdict"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Run the architect on a fixture prompt; assert it emits a typed ADR proposal with sections {Context,Decision,Consequences}.
