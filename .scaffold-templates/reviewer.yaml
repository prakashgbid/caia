# Scaffold template for @chiefaia/reviewer
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_REVIEWER
title: "Typical work shape — reviewer"
description: |
  Package: @chiefaia/reviewer
  Survey-line: Craftsmanship-focused PR review agent. Reads PR diffs, runs deterministic pattern detectors plus LLM-reasoned craftsmanship review (claude binary, subscription-only) for readability/idiom/maintainabil

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/reviewer/src/<feature>.ts
  - packages/reviewer/tests/<feature>.test.ts
  - packages/reviewer/README.md
success_criteria:
  output_file: reports/caia/reviewer-<feature>.md
  min_bytes: 1500
  grep_match: "verdict|findings"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Review a fixture PR diff; assert verdict in {APPROVE,REQUEST_CHANGES} and findings array is well-formed JSON.
