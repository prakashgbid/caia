# Scaffold template for @chiefaia/mcp-allowlist-proxy
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_MCP_ALLOWLIST_PROXY
title: "Typical work shape — mcp-allowlist-proxy"
description: |
  Package: @chiefaia/mcp-allowlist-proxy
  Survey-line: Deterministic command-allowlist proxy in front of an MCP server. Pins the upstream commit SHA, wraps spawn in macOS sandbox-exec, and rejects any tool call not on the policy allowlist before forwardin

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/mcp-allowlist-proxy/src/<feature>.ts
  - packages/mcp-allowlist-proxy/tests/<feature>.test.ts
  - packages/mcp-allowlist-proxy/README.md
success_criteria:
  output_file: reports/stolution/mcp-allowlist-proxy-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  pnpm --filter @chiefaia/mcp-allowlist-proxy build && pnpm --filter @chiefaia/mcp-allowlist-proxy test; both exit 0, vitest summary shows 0 failures.
