# Scaffold template for @chiefaia/guardrails-validator
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_GUARDRAILS_VALIDATOR
title: "Typical work shape — guardrails-validator"
description: |
  Package: @chiefaia/guardrails-validator
  Survey-line: Layer-2 input/output validation for agent LLM calls. Pure-TS, all-local, no API keys, no Python bridge. Composable validator profiles (untrusted-user-input, inter-agent, pre-publish, tool-call-args). 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/guardrails-validator/src/<feature>.ts
  - packages/guardrails-validator/tests/<feature>.test.ts
  - packages/guardrails-validator/README.md
success_criteria:
  output_file: reports/caia/guardrails-validator-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run guardrail profile on adversarial inputs; assert prompt-injection markers stripped and PII redacted as expected.
