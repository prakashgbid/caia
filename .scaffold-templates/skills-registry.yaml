# Scaffold template for @chiefaia/skills-registry
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_SKILLS_REGISTRY
title: "Typical work shape — skills-registry"
description: |
  Package: @chiefaia/skills-registry
  Survey-line: Foundational typed registry for agent skills (capabilities). Lets agents declare what they can do, lets the orchestrator query/match by capability + tags + cost class, and provides an in-memory store 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/skills-registry/src/<feature>.ts
  - packages/skills-registry/tests/<feature>.test.ts
  - packages/skills-registry/README.md
success_criteria:
  output_file: reports/caia/skills-registry-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Seed N rows; assert query(byCapability) returns the expected subset; assert insert is idempotent on same id.
