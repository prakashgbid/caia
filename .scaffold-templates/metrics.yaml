# Scaffold template for @chiefaia/metrics
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_METRICS
title: "Typical work shape — metrics"
description: |
  Package: @chiefaia/metrics
  Survey-line: Prometheus-compatible application metrics

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/metrics/src/<feature>.ts
  - packages/metrics/tests/<feature>.test.ts
  - packages/metrics/README.md
success_criteria:
  output_file: reports/caia/metrics-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Curl /metrics; assert at least one counter, one gauge, one histogram emitted with expected labels.
