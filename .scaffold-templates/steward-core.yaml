# Scaffold template for @chiefaia/steward-core
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_STEWARD_CORE
title: "Typical work shape — steward-core"
description: |
  Package: @chiefaia/steward-core
  Survey-line: DevOps Steward Agent — process-graph evaluator (propose-only, P0). Codifies post-release back-merge expectation as the first watcher; foundation for the continuous-compliance Steward.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/steward-core/src/<feature>.ts
  - packages/steward-core/tests/<feature>.test.ts
  - packages/steward-core/README.md
success_criteria:
  output_file: reports/caia/steward-core-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Evaluate the Steward process graph against a fixture PR-merge event; assert recommendation (propose-only) is emitted.
