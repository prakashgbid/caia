# Scaffold template for @chiefaia/dedup-engine
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_DEDUP_ENGINE
title: "Typical work shape — dedup-engine"
description: |
  Package: @chiefaia/dedup-engine
  Survey-line: Deduplication engine — Jaccard similarity with temporal decay.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 2
file_paths:
  - packages/dedup-engine/src/<feature>.ts
  - packages/dedup-engine/tests/<feature>.test.ts
  - packages/dedup-engine/README.md
success_criteria:
  output_file: reports/caia/dedup-engine-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Dedup a fixture corpus; assert duplicates collapse with cosine-sim >= 0.8 and temporal decay applied as configured.
