# Scaffold template for @chiefaia/ticket-template
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_TICKET_TEMPLATE
title: "Typical work shape — ticket-template"
description: |
  Package: @chiefaia/ticket-template
  Survey-line: Strict, Zod-validated ticket template (v1) — the contract between Phase 1 agents (PO, BA, EA, DBA, BFF, UI, Security, Testing, Release) and the executor.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 82
file_paths:
  - packages/ticket-template/src/<feature>.ts
  - packages/ticket-template/tests/<feature>.test.ts
  - packages/ticket-template/README.md
success_criteria:
  output_file: reports/caia/ticket-template-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Vitest case validates fixture inputs; assert all canonical examples pass and at least 3 negative cases reject with field-paths.
