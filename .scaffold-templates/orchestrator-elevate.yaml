# Scaffold template for @chiefaia/orchestrator-elevate
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_ORCHESTRATOR_ELEVATE
title: "Typical work shape — orchestrator-elevate"
description: |
  Package: @chiefaia/orchestrator-elevate
  Survey-line: Root-owned sudo wrapper + scoped Vault AppRole for permanent Cowork orchestrator privilege escalation on stolution. Single NOPASSWD entry point with exhaustive allowlisting and JSONL audit logging.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/orchestrator-elevate/src/<feature>.ts
  - packages/orchestrator-elevate/tests/<feature>.test.ts
  - packages/orchestrator-elevate/README.md
success_criteria:
  output_file: reports/stolution/orchestrator-elevate-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Invoke a NOPASSWD-allowlisted command via the wrapper; assert it runs and a denied command returns auth error.
