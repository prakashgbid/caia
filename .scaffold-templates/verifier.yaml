# Scaffold template for @chiefaia/verifier
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_VERIFIER
title: "Typical work shape — verifier"
description: |
  Package: @chiefaia/verifier
  Survey-line: VERIFIER review-sibling — fourth sibling alongside Critic, Code-Reviewer, Reviewer. Spawns a fresh-prompt, fresh-worktree claude binary that re-derives acceptance-criteria/test/file-scope verdicts fro

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/verifier/src/<feature>.ts
  - packages/verifier/tests/<feature>.test.ts
  - packages/verifier/README.md
success_criteria:
  output_file: reports/stolution/verifier-<feature>.md
  min_bytes: 1500
  grep_match: "verdict|findings"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Review a fixture PR diff; assert verdict in {APPROVE,REQUEST_CHANGES} and findings array is well-formed JSON.
