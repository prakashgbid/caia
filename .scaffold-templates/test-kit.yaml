# Scaffold template for @chiefaia/test-kit
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_TEST_KIT
title: "Typical work shape — test-kit"
description: |
  Package: @chiefaia/test-kit
  Survey-line: Test utilities, mocks, and fixtures for CAIA packages

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/test-kit/src/<feature>.ts
  - packages/test-kit/tests/<feature>.test.ts
  - packages/test-kit/README.md
success_criteria:
  output_file: reports/caia/test-kit-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Load every fixture exported by the kit; assert all parse cleanly and the snapshot diff is empty.
