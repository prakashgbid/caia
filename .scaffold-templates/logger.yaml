# Scaffold template for @chiefaia/logger
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LOGGER
title: "Typical work shape — logger"
description: |
  Package: @chiefaia/logger
  Survey-line: Structured logging (Pino-backed) for CAIA applications

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 27
file_paths:
  - packages/logger/src/<feature>.ts
  - packages/logger/tests/<feature>.test.ts
  - packages/logger/README.md
success_criteria:
  output_file: reports/caia/logger-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Vitest case writes a sample log; assert JSON shape contains {level,msg,ts,svc} and pid is omitted in production preset.
