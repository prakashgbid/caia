# Scaffold template for @chiefaia/image-provider
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_IMAGE_PROVIDER
title: "Typical work shape — image-provider"
description: |
  Package: @chiefaia/image-provider
  Survey-line: Supply real photo-quality imagery to roulette-community and poker-zeno sites

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/image-provider/src/<feature>.ts
  - packages/image-provider/tests/<feature>.test.ts
  - packages/image-provider/README.md
success_criteria:
  output_file: reports/stolution/image-provider-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Fetch one image from each provider; assert content-type image/* and bytes > 10KB; cache hit on second fetch.
