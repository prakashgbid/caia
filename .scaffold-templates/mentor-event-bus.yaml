# Scaffold template for @chiefaia/mentor-event-bus
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_MENTOR_EVENT_BUS
title: "Typical work shape — mentor-event-bus"
description: |
  Package: @chiefaia/mentor-event-bus
  Survey-line: Typed append-only event substrate for Mentor Phase 0. SQLite-backed events table with Zod-validated schemas and AsyncLocalStorage-based correlation IDs. Producers emit; Mentor / Curator / Steward quer

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 17
file_paths:
  - packages/mentor-event-bus/src/<feature>.ts
  - packages/mentor-event-bus/tests/<feature>.test.ts
  - packages/mentor-event-bus/README.md
success_criteria:
  output_file: reports/stolution/mentor-event-bus-<feature>.md
  min_bytes: 1200
  grep_match: "emit|schema_version|events table"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Vitest case emits a synthetic event of every typed shape; integration test asserts SQLite events table contains the new row with the expected schema_version.
