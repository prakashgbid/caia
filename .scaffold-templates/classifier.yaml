# Scaffold template for @chiefaia/classifier
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CLASSIFIER
title: "Typical work shape — classifier"
description: |
  Package: @chiefaia/classifier
  Survey-line: Domain taxonomy engine — classifies text into functional domains and assigns labels.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 7
file_paths:
  - packages/classifier/src/<feature>.ts
  - packages/classifier/tests/<feature>.test.ts
  - packages/classifier/README.md
success_criteria:
  output_file: reports/caia/classifier-<feature>.md
  min_bytes: 2000
  grep_match: "pass rate|baseline|canonical-suite"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Classify a fixture batch; assert accuracy on canonical-suite-v2 >= prior baseline; assert no unknown-intent leaks.
