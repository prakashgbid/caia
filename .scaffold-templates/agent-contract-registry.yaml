# Scaffold template for @chiefaia/agent-contract-registry
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_AGENT_CONTRACT_REGISTRY
title: "Typical work shape — agent-contract-registry"
description: |
  Package: @chiefaia/agent-contract-registry
  Survey-line: Agent Section Contract Registry — declarative store of per-agent contracts (PO/BA/EA/Test-Design) + composeTemplate(scope) function. The Story Validator consumes the composed template at runtime inste

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 12
file_paths:
  - packages/agent-contract-registry/src/<feature>.ts
  - packages/agent-contract-registry/tests/<feature>.test.ts
  - packages/agent-contract-registry/README.md
success_criteria:
  output_file: reports/caia/agent-contract-registry-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Seed N rows; assert query(byCapability) returns the expected subset; assert insert is idempotent on same id.
