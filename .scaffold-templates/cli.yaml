# Scaffold template for @chiefaia/cli
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CLI
title: "Typical work shape — cli"
description: |
  Package: @chiefaia/cli
  Survey-line: CAIA CLI — scaffold utilities, sites, and agents

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/cli/src/<feature>.ts
  - packages/cli/tests/<feature>.test.ts
  - packages/cli/README.md
success_criteria:
  output_file: reports/caia/cli-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run `pnpm --filter @chiefaia/cli cli --help`; assert exit 0 and the top-level command list is present.
