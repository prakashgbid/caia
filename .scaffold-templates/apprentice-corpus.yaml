# Scaffold template for @chiefaia/apprentice-corpus
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_APPRENTICE_CORPUS
title: "Typical work shape — apprentice-corpus"
description: |
  Package: @chiefaia/apprentice-corpus
  Survey-line: Apprentice Phase 0 — corpus aggregator. Walks CAIA's accumulated artifacts (Mentor events, agent/memory directives + feedback, reports, GitHub PR history, Langfuse traces) and normalises them into a u

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/apprentice-corpus/src/<feature>.ts
  - packages/apprentice-corpus/tests/<feature>.test.ts
  - packages/apprentice-corpus/README.md
success_criteria:
  output_file: reports/stolution/apprentice-corpus-<feature>.md
  min_bytes: 2000
  grep_match: "adapter|loss|epoch|samples.jsonl"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Aggregate fixture artifacts into samples.jsonl; assert >= 500 samples, valid JSONL, and manifest checksum matches.
