# Scaffold template for @pokerzeno/cast-bridge
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CAST_BRIDGE
title: "Typical work shape — cast-bridge"
description: |
  Package: @pokerzeno/cast-bridge
  Survey-line: Reusable casting utility for PokerZeno and Roulette Community — BroadcastChannel two-tab sync + Chrome tab-mirror casting

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/cast-bridge/src/<feature>.ts
  - packages/cast-bridge/tests/<feature>.test.ts
  - packages/cast-bridge/README.md
success_criteria:
  output_file: reports/stolution/cast-bridge-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Open two tabs sharing a BroadcastChannel; assert state mutation in tab A is visible in tab B within 200ms.
