# Scaffold template for @chiefaia/claude-spawner
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CLAUDE_SPAWNER
title: "Typical work shape — claude-spawner"
description: |
  Package: @chiefaia/claude-spawner
  Survey-line: Unified subscription-only `claude` binary spawner. Replaces the ad-hoc spawn implementations across the CAIA monorepo (verifier, code-reviewer, critic, reviewer, apprentice-eval, apprentice-corpus dis

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 21
file_paths:
  - packages/claude-spawner/src/<feature>.ts
  - packages/claude-spawner/tests/<feature>.test.ts
  - packages/claude-spawner/README.md
success_criteria:
  output_file: reports/stolution/claude-spawner-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Spawn a claude subprocess with a no-op prompt; assert exit code 0 and stdout contains the canonical handshake line.
