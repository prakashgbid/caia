# Scaffold template for @chiefaia/story-decomposer
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_STORY_DECOMPOSER
title: "Typical work shape — story-decomposer"
description: |
  Package: @chiefaia/story-decomposer
  Survey-line: 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/story-decomposer/src/<feature>.ts
  - packages/story-decomposer/tests/<feature>.test.ts
  - packages/story-decomposer/README.md
success_criteria:
  output_file: reports/caia/story-decomposer-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Decompose a fixture initiative; assert output has Initiative → Epic → Story shape and MECE-judge passes.
