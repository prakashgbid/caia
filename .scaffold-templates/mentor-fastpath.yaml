# Scaffold template for @chiefaia/mentor-fastpath
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_MENTOR_FASTPATH
title: "Typical work shape — mentor-fastpath"
description: |
  Package: @chiefaia/mentor-fastpath
  Survey-line: Mentor Phase-1 reactive fast-path + Phase-2 postmerge data layer + Phase-2 postmerge watcher daemon + Phase-2 postmerge consumer. Phase 1 subscribes to OperatorCorrection events and writes proposals. 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/mentor-fastpath/src/<feature>.ts
  - packages/mentor-fastpath/tests/<feature>.test.ts
  - packages/mentor-fastpath/README.md
success_criteria:
  output_file: reports/stolution/mentor-fastpath-<feature>.md
  min_bytes: 1200
  grep_match: "emit|schema_version|events table"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Vitest case emits a synthetic event of every typed shape; integration test asserts SQLite events table contains the new row with the expected schema_version.
