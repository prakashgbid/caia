# Scaffold template for @chiefaia/mentor-retrieval
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_MENTOR_RETRIEVAL
title: "Typical work shape — mentor-retrieval"
description: |
  Package: @chiefaia/mentor-retrieval
  Survey-line: Mentor Phase-3 + Phase-4 lesson retrieval — embed past feedback_*.md + proposals/*.md via Ollama nomic-embed-text, persist to a per-memoryDir SQLite index, retrieve top-N most similar prior lessons, p

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/mentor-retrieval/src/<feature>.ts
  - packages/mentor-retrieval/tests/<feature>.test.ts
  - packages/mentor-retrieval/README.md
success_criteria:
  output_file: reports/stolution/mentor-retrieval-<feature>.md
  min_bytes: 1500
  grep_match: "cosine|hit rate|index"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Embed and index N fixture docs; assert query returns top-K with cosine-sim above threshold; assert index file written atomically.
