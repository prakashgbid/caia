# Scaffold template for @chiefaia/local-llm-router
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LOCAL_LLM_ROUTER
title: "Typical work shape — local-llm-router"
description: |
  Package: @chiefaia/local-llm-router
  Survey-line: LLM routing layer — dispatches tasks to local Ollama models or Claude API based on complexity and cost rules

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 21
file_paths:
  - packages/local-llm-router/src/<feature>.ts
  - packages/local-llm-router/tests/<feature>.test.ts
  - packages/local-llm-router/README.md
success_criteria:
  output_file: reports/stolution/local-llm-router-<feature>.md
  min_bytes: 1500
  grep_match: "healthz|kill -TERM|exit 0"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Start daemon (pnpm --filter @chiefaia/local-llm-router dev), curl /healthz returns 200, kill -TERM exits cleanly within 5s.
