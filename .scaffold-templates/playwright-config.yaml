# Scaffold template for @chiefaia/playwright-config
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_PLAYWRIGHT_CONFIG
title: "Typical work shape — playwright-config"
description: |
  Package: @chiefaia/playwright-config
  Survey-line: Shared Playwright config factory for the Fix-It Test Agent — local workers + remote Browserless mode

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/playwright-config/src/<feature>.ts
  - packages/playwright-config/tests/<feature>.test.ts
  - packages/playwright-config/README.md
success_criteria:
  output_file: reports/caia/playwright-config-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run a single Playwright spec against local fixture; assert page screenshot captured + 0 console errors.
