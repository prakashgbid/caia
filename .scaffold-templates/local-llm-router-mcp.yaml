# Scaffold template for @chiefaia/local-llm-router-mcp
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LOCAL_LLM_ROUTER_MCP
title: "Typical work shape — local-llm-router-mcp"
description: |
  Package: @chiefaia/local-llm-router-mcp
  Survey-line: MCP stdio server that exposes the local-llm-router daemon as 5 Cowork tools (local_classify, local_summarize, local_draft, local_format, local_search_memory).

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/local-llm-router-mcp/src/<feature>.ts
  - packages/local-llm-router-mcp/tests/<feature>.test.ts
  - packages/local-llm-router-mcp/README.md
success_criteria:
  output_file: reports/stolution/local-llm-router-mcp-<feature>.md
  min_bytes: 1500
  grep_match: "healthz|kill -TERM|exit 0"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Start daemon (pnpm --filter @chiefaia/local-llm-router-mcp dev), curl /healthz returns 200, kill -TERM exits cleanly within 5s.
