# Scaffold template for @chiefaia/chain-runner
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CHAIN_RUNNER
title: "Typical work shape — chain-runner"
description: |
  Package: @chiefaia/chain-runner
  Survey-line: Multi-phase chain runner for long-running autonomous Claude work — atomic state machine, lockfile + heartbeat, audit log, and CLI dispatched by a scheduled wake.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/chain-runner/src/<feature>.ts
  - packages/chain-runner/tests/<feature>.test.ts
  - packages/chain-runner/README.md
success_criteria:
  output_file: reports/stolution/chain-runner-<feature>.md
  min_bytes: 1500
  grep_match: "healthz|kill -TERM|exit 0"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Start daemon (pnpm --filter @chiefaia/chain-runner dev), curl /healthz returns 200, kill -TERM exits cleanly within 5s.
