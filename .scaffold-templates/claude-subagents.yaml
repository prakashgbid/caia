# Scaffold template for @chiefaia/claude-subagents
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CLAUDE_SUBAGENTS
title: "Typical work shape — claude-subagents"
description: |
  Package: @chiefaia/claude-subagents
  Survey-line: CAIA-flavoured Claude Code subagent definitions + installer. Ships canonical .md system prompts for the BA / EA / Validator / Test-Design / Coding / Fix-It / Steward / Mentor / Curator / PO agent role

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: lightly-integrated
importers_seen: 1
file_paths:
  - packages/claude-subagents/src/<feature>.ts
  - packages/claude-subagents/tests/<feature>.test.ts
  - packages/claude-subagents/README.md
success_criteria:
  output_file: reports/stolution/claude-subagents-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Spawn a claude subprocess with a no-op prompt; assert exit code 0 and stdout contains the canonical handshake line.
