# Scaffold template for @chiefaia/llm-cache
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LLM_CACHE
title: "Typical work shape — llm-cache"
description: |
  Package: @chiefaia/llm-cache
  Survey-line: Two-tier prompt cache (exact + semantic) for the local LLM router. Sqlite-backed, embedder-pluggable. Ships an L6 cascade-tier preset (24h TTL, 0.92 cosine threshold) backed by nomic-embed-text via Ol

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/llm-cache/src/<feature>.ts
  - packages/llm-cache/tests/<feature>.test.ts
  - packages/llm-cache/README.md
success_criteria:
  output_file: reports/stolution/llm-cache-<feature>.md
  min_bytes: 1500
  grep_match: "cosine|hit rate|index"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run same prompt twice; assert second call is a cache-hit in /admin/metrics; assert wall-clock < 50ms on hit.
