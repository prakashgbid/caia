# Scaffold template for @pokerzeno/content-engine
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CONTENT_ENGINE
title: "Typical work shape — content-engine"
description: |
  Package: @pokerzeno/content-engine
  Survey-line: Automated content generation for PokerZeno and Roulette Community

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/content-engine/src/<feature>.ts
  - packages/content-engine/tests/<feature>.test.ts
  - packages/content-engine/README.md
success_criteria:
  output_file: reports/stolution/content-engine-<feature>.md
  min_bytes: 2000
  grep_match: "report|crawl|digest"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Generate one article end-to-end; assert >= 800 words, passes guardrails-validator, persisted to Supabase fixture.
