# Scaffold template for @chiefaia/vastu
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_VASTU
title: "Typical work shape — vastu"
description: |
  Package: @chiefaia/vastu
  Survey-line: VASTU pipeline — text → formal page brief → Figma spec → Next.js scaffold. Private @chiefaia/* package per Option E (agent_architecture_shape_2026-05-06.md). Ports the shape of Stolution's @stolution/

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: lightly-integrated
importers_seen: 1
file_paths:
  - packages/vastu/src/<feature>.ts
  - packages/vastu/tests/<feature>.test.ts
  - packages/vastu/README.md
success_criteria:
  output_file: reports/caia/vastu-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Convert one text brief → Figma JSON → Next.js scaffold; assert generated component compiles with tsc --noEmit.
