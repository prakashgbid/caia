# Scaffold template for @chiefaia/apprentice-training
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_APPRENTICE_TRAINING
title: "Typical work shape — apprentice-training"
description: |
  Package: @chiefaia/apprentice-training
  Survey-line: Apprentice Phase 2 — LoRA training pipeline. Reads `@chiefaia/apprentice-corpus` manifest + samples.jsonl, splits into train/valid/test (honouring the corpus manifest's deterministic holdout when pres

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/apprentice-training/src/<feature>.ts
  - packages/apprentice-training/tests/<feature>.test.ts
  - packages/apprentice-training/README.md
success_criteria:
  output_file: reports/stolution/apprentice-training-<feature>.md
  min_bytes: 2000
  grep_match: "adapter|loss|epoch|samples.jsonl"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Run one short training epoch on a fixture corpus; assert adapter file produced and metrics.json contains loss curve.
