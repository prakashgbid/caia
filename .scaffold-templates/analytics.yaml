# Scaffold template for @chiefaia/analytics
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_ANALYTICS
title: "Typical work shape — analytics"
description: |
  Package: @chiefaia/analytics
  Survey-line: Shared GA4 analytics, consent management, and event taxonomy for Pokerzeno sites

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/analytics/src/<feature>.ts
  - packages/analytics/tests/<feature>.test.ts
  - packages/analytics/README.md
success_criteria:
  output_file: reports/stolution/analytics-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Fire one consented event; assert payload shape against taxonomy; assert non-consented call is a no-op.
