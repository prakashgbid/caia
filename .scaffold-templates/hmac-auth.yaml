# Scaffold template for @chiefaia/hmac-auth
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_HMAC_AUTH
title: "Typical work shape — hmac-auth"
description: |
  Package: @chiefaia/hmac-auth
  Survey-line: Shared HMAC-SHA256 primitives and timestamped request signing for CAIA services. Lifted from @chiefaia/mentor-event-bus auth so the same hardened implementation backs both event-bus HTTP request signi

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 6
file_paths:
  - packages/hmac-auth/src/<feature>.ts
  - packages/hmac-auth/tests/<feature>.test.ts
  - packages/hmac-auth/README.md
success_criteria:
  output_file: reports/caia/hmac-auth-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Sign a request, verify signature; assert tamper detection on body + replay-window enforcement on timestamp.
