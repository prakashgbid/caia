# Scaffold template for @chiefaia/critic
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CRITIC
title: "Typical work shape — critic"
description: |
  Package: @chiefaia/critic
  Survey-line: Pre-commit adversarial review agent. Reads PR diffs, runs deterministic pattern detectors plus LLM-reasoned adversarial review (claude binary, subscription-only) against Mentor's 18-category failure-m

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/critic/src/<feature>.ts
  - packages/critic/tests/<feature>.test.ts
  - packages/critic/README.md
success_criteria:
  output_file: reports/caia/critic-<feature>.md
  min_bytes: 1500
  grep_match: "verdict|findings"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Review a fixture PR diff; assert verdict in {APPROVE,REQUEST_CHANGES} and findings array is well-formed JSON.
