# Scaffold template for @chiefaia/test-isolation
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_TEST_ISOLATION
title: "Typical work shape — test-isolation"
description: |
  Package: @chiefaia/test-isolation
  Survey-line: Per-test ephemeral SQLite + isolated localhost ports for parallel test runs

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/test-isolation/src/<feature>.ts
  - packages/test-isolation/tests/<feature>.test.ts
  - packages/test-isolation/README.md
success_criteria:
  output_file: reports/caia/test-isolation-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run 8 vitest workers in parallel; assert no port collision and no SQLite file collision across workers.
