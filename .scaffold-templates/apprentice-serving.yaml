# Scaffold template for @chiefaia/apprentice-serving
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_APPRENTICE_SERVING
title: "Typical work shape — apprentice-serving"
description: |
  Package: @chiefaia/apprentice-serving
  Survey-line: Apprentice Phase 3 — adapter swapping + serving. Loads Phase 2 adapter directories into Ollama via subprocess `ollama create`, tracks each adapter's lifecycle state (registered → shadow → canary → pro

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/apprentice-serving/src/<feature>.ts
  - packages/apprentice-serving/tests/<feature>.test.ts
  - packages/apprentice-serving/README.md
success_criteria:
  output_file: reports/stolution/apprentice-serving-<feature>.md
  min_bytes: 2000
  grep_match: "adapter|loss|epoch|samples.jsonl"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Swap one adapter into a running Ollama instance; assert generation uses the new adapter (probe prompt expected output).
