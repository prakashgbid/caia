# Scaffold template for @chiefaia/capability-broker
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CAPABILITY_BROKER
title: "Typical work shape — capability-broker"
description: |
  Package: @chiefaia/capability-broker
  Survey-line: Capability-token broker for irreversible agent actions (cf api, supabase admin, npm publish, git push, gh pr merge). Issues short-lived signed tokens and records every privileged execution to an appen

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 7
file_paths:
  - packages/capability-broker/src/<feature>.ts
  - packages/capability-broker/tests/<feature>.test.ts
  - packages/capability-broker/README.md
success_criteria:
  output_file: reports/caia/capability-broker-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Mint a capability token, exercise irreversible action; assert single-use enforcement and audit-event emitted.
