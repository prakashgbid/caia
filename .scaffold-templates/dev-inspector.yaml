# Scaffold template for @chiefaia/dev-inspector
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_DEV_INSPECTOR
title: "Typical work shape — dev-inspector"
description: |
  Package: @chiefaia/dev-inspector
  Survey-line: Dev-only React element inspector: hover outline, stable fiber IDs, click-to-copy

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/dev-inspector/src/<feature>.ts
  - packages/dev-inspector/tests/<feature>.test.ts
  - packages/dev-inspector/README.md
success_criteria:
  output_file: reports/caia/dev-inspector-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Inject inspector into fixture page; assert fiber-id stable across re-render and copy-to-clipboard payload is valid JSON.
