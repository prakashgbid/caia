# Scaffold template for @chiefaia/feature-registry
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_FEATURE_REGISTRY
title: "Typical work shape — feature-registry"
description: |
  Package: @chiefaia/feature-registry
  Survey-line: Fast, local, token-cheap feature registry. PO Agent queries it before classifying lifecycle (new vs enhance). Hybrid sqlite-vec + FTS5 search; nomic-embed-text via Ollama; zero Claude tokens per class

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 18
file_paths:
  - packages/feature-registry/src/<feature>.ts
  - packages/feature-registry/tests/<feature>.test.ts
  - packages/feature-registry/README.md
success_criteria:
  output_file: reports/caia/feature-registry-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Seed N rows; assert query(byCapability) returns the expected subset; assert insert is idempotent on same id.
