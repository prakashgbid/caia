# Scaffold template for @chiefaia/apprentice-retrainer
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_APPRENTICE_RETRAINER
title: "Typical work shape — apprentice-retrainer"
description: |
  Package: @chiefaia/apprentice-retrainer
  Survey-line: Apprentice Phase 4 — continuous retraining cron. Weekly Saturday 02:00 + threshold-trigger (≥500 new pairs OR ≥7 days). Orchestrates corpus → train → eval → serving end-to-end; auto-promotes-to-canary

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/apprentice-retrainer/src/<feature>.ts
  - packages/apprentice-retrainer/tests/<feature>.test.ts
  - packages/apprentice-retrainer/README.md
success_criteria:
  output_file: reports/stolution/apprentice-retrainer-<feature>.md
  min_bytes: 2000
  grep_match: "adapter|loss|epoch|samples.jsonl"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Trigger the retrainer cron once; assert it picks up the latest corpus, runs end-to-end, and writes a new adapter dir.
