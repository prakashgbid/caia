# Scaffold template for @chiefaia/prompt-optimizer
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_PROMPT_OPTIMIZER
title: "Typical work shape — prompt-optimizer"
description: |
  Package: @chiefaia/prompt-optimizer
  Survey-line: Three-stage prompt optimizer: rule-based prepass + tool-output summarize + token-importance prune. Sits in front of every model call routed through @chiefaia/local-llm-router.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 5
file_paths:
  - packages/prompt-optimizer/src/<feature>.ts
  - packages/prompt-optimizer/tests/<feature>.test.ts
  - packages/prompt-optimizer/README.md
success_criteria:
  output_file: reports/caia/prompt-optimizer-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Optimize a verbose prompt; assert token count drops >= 20% and semantic equivalence holds on canonical-suite eval.
