# Scaffold template for @chiefaia/apprentice-eval
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_APPRENTICE_EVAL
title: "Typical work shape — apprentice-eval"
description: |
  Package: @chiefaia/apprentice-eval
  Survey-line: Apprentice Phase 1 — eval harness. Scores the Apprentice base model + each candidate LoRA adapter against a canonical YAML prompt suite (hand-curated CAIA-vocabulary prompts + corpus-holdout + Mentor-

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/apprentice-eval/src/<feature>.ts
  - packages/apprentice-eval/tests/<feature>.test.ts
  - packages/apprentice-eval/README.md
success_criteria:
  output_file: reports/stolution/apprentice-eval-<feature>.md
  min_bytes: 2000
  grep_match: "pass rate|baseline|canonical-suite"
requires_merged_pr: false
phase_count: 3
deps: []
demonstrate_step: |
  Run the full eval suite; assert pass rate >= baseline (no regression) and a per-prompt score table is written.
