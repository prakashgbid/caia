# Scaffold template for @chiefaia/dspy-bridge
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_DSPY_BRIDGE
title: "Typical work shape — dspy-bridge"
description: |
  Package: @chiefaia/dspy-bridge
  Survey-line: Node ↔ Python bridge for DSPy. Spawns a uv-pinned Python sub-process that hosts compiled DSPy programs and answers JSON-line RPC. Substrate pick #1 of the AI tech modernization proposal §6.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/dspy-bridge/src/<feature>.ts
  - packages/dspy-bridge/tests/<feature>.test.ts
  - packages/dspy-bridge/README.md
success_criteria:
  output_file: reports/stolution/dspy-bridge-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Start the DSPy sub-process; round-trip a minimal program; assert exit-code 0 and uv-pinned interpreter version.
