# Scaffold template for @chiefaia/researcher
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_RESEARCHER
title: "Typical work shape — researcher"
description: |
  Package: @chiefaia/researcher
  Survey-line: Researcher Agent — on-demand deep-dive technology evaluation. Decomposes a query into sub-questions, fetches multi-source evidence (WebSearch + WebFetch + Librarian precedent), spawns the claude binar

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/researcher/src/<feature>.ts
  - packages/researcher/tests/<feature>.test.ts
  - packages/researcher/README.md
success_criteria:
  output_file: reports/stolution/researcher-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: false
phase_count: 2
deps: []
demonstrate_step: |
  Run a deep-dive query; assert evidence doc has >= 3 cited sources, >= 1500 bytes, and conclusion section.
