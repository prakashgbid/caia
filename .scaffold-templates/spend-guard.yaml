# Scaffold template for @chiefaia/spend-guard
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_SPEND_GUARD
title: "Typical work shape — spend-guard"
description: |
  Package: @chiefaia/spend-guard
  Survey-line: Programmatic Anthropic-spend cap with hard auto-pause + serial account-pool fallback (per v2 §6). Tracks token spend in real-time via response headers, enforces per-task / per-project / global-day / g

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 4
file_paths:
  - packages/spend-guard/src/<feature>.ts
  - packages/spend-guard/tests/<feature>.test.ts
  - packages/spend-guard/README.md
success_criteria:
  output_file: reports/caia/spend-guard-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Drive spend over cap in fixture; assert pause triggers, account fallback engages, and resume after reset works.
