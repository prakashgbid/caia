# Scaffold template for @chiefaia/secrets-broker
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_SECRETS_BROKER
title: "Typical work shape — secrets-broker"
description: |
  Package: @chiefaia/secrets-broker
  Survey-line: 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/secrets-broker/src/<feature>.ts
  - packages/secrets-broker/tests/<feature>.test.ts
  - packages/secrets-broker/README.md
success_criteria:
  output_file: reports/caia/secrets-broker-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Vitest case reads a fixture secret via the client; assert env-var fallback path works when broker is offline.
