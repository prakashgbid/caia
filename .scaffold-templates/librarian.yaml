# Scaffold template for @chiefaia/librarian
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LIBRARIAN
title: "Typical work shape — librarian"
description: |
  Package: @chiefaia/librarian
  Survey-line: Librarian Agent — pre-spawn precedent retrieval. Embeds the entire memory + reports corpus (directives, registries, architecture docs, master plans, landscape research, gate completion reports, handof

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: live
importers_seen: 3
file_paths:
  - packages/librarian/src/<feature>.ts
  - packages/librarian/tests/<feature>.test.ts
  - packages/librarian/README.md
success_criteria:
  output_file: reports/stolution/librarian-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: false
phase_count: 1
deps: []
demonstrate_step: |
  Run a precedent query; assert top-K results cite at least one ADR + one prior incident, with confidence labels.
