# Scaffold template for @chiefaia/local-llm-router-py-client
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_LOCAL_LLM_ROUTER_PY_CLIENT
title: "Typical work shape — local-llm-router-py-client"
description: |
  Package: @chiefaia/local-llm-router-py-client
  Survey-line: Python client + B15.C v2 spawn prompt template + SPS-α claude-argv builder (headroom-wrap + KV-cache prefix stabilization). Vendored into claude-spawner-agent on M1+M3+stolution. Pure stdlib (no pip d

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/local-llm-router-py-client/src/<feature>.ts
  - packages/local-llm-router-py-client/tests/<feature>.test.ts
  - packages/local-llm-router-py-client/README.md
success_criteria:
  output_file: reports/stolution/local-llm-router-py-client-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  pnpm --filter @chiefaia/local-llm-router-py-client build && pnpm --filter @chiefaia/local-llm-router-py-client test; both exit 0, vitest summary shows 0 failures.
