# Scaffold template for @chiefaia/architecture-registry
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_ARCHITECTURE_REGISTRY
title: "Typical work shape — architecture-registry"
description: |
  Package: @chiefaia/architecture-registry
  Survey-line: Architecture Knowledge Graph (AKG): structured catalog of every architectural artifact in the CAIA monorepo + sites — services, APIs, components, themes, plugins, packages, schemas, migrations, integr

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 10
file_paths:
  - packages/architecture-registry/src/<feature>.ts
  - packages/architecture-registry/tests/<feature>.test.ts
  - packages/architecture-registry/README.md
success_criteria:
  output_file: reports/caia/architecture-registry-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Seed N rows; assert query(byCapability) returns the expected subset; assert insert is idempotent on same id.
