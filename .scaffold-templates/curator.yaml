# Scaffold template for @chiefaia/curator
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_CURATOR
title: "Typical work shape — curator"
description: |
  Package: @chiefaia/curator
  Survey-line: Curator Phase-2 — daily-running CAIA agent that scans the platform across measurable quality dimensions and emits both a daily digest of findings AND an action layer (PR proposals, backlog directives,

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/curator/src/<feature>.ts
  - packages/curator/tests/<feature>.test.ts
  - packages/curator/README.md
success_criteria:
  output_file: reports/stolution/curator-<feature>.md
  min_bytes: 2000
  grep_match: "report|crawl|digest"
requires_merged_pr: false
phase_count: 3
deps: []
demonstrate_step: |
  Run the daily curator scan; assert digest markdown written with date stamp and at least one section per quality dimension.
