# Scaffold template for @chiefaia/surface
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_SURFACE
title: "Typical work shape — surface"
description: |
  Package: @chiefaia/surface
  Survey-line: Surface Agent — operator-curation lens. Filters and digests important findings from PR activity, agent-memory deltas, and agent transcripts so the operator gets a curated 'what matters this week' view

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/surface/src/<feature>.ts
  - packages/surface/tests/<feature>.test.ts
  - packages/surface/README.md
success_criteria:
  output_file: reports/caia/surface-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run the Surface scan over fixture activity; assert digest markdown lists curated findings grouped by surface.
