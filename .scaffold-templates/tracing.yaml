# Scaffold template for @chiefaia/tracing
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_TRACING
title: "Typical work shape — tracing"
description: |
  Package: @chiefaia/tracing
  Survey-line: OpenTelemetry distributed tracing

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/tracing/src/<feature>.ts
  - packages/tracing/tests/<feature>.test.ts
  - packages/tracing/README.md
success_criteria:
  output_file: reports/caia/tracing-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Span emitted with parent context, captured by in-memory OTLP exporter; assert trace_id propagates.
