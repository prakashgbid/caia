# Scaffold template for @pokerzeno/backend-core
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_BACKEND_CORE
title: "Typical work shape — backend-core"
description: |
  Package: @pokerzeno/backend-core
  Survey-line: Shared Supabase backend for RouletteCommunity and PokerZeno

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/backend-core/src/<feature>.ts
  - packages/backend-core/tests/<feature>.test.ts
  - packages/backend-core/README.md
success_criteria:
  output_file: reports/stolution/backend-core-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run a query against fixture Supabase schema; assert RLS policy denies cross-tenant read and allows owner read.
