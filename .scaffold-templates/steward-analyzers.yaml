# Scaffold template for @chiefaia/steward-analyzers
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_STEWARD_ANALYZERS
title: "Typical work shape — steward-analyzers"
description: |
  Package: @chiefaia/steward-analyzers
  Survey-line: Static analyzers for the Steward Gatekeeper. Pre-merge checks that are not predicate-evaluable on the steward-core engine: Drizzle migration breakpoint linter, migration-numbering collision/gap scanne

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/steward-analyzers/src/<feature>.ts
  - packages/steward-analyzers/tests/<feature>.test.ts
  - packages/steward-analyzers/README.md
success_criteria:
  output_file: reports/caia/steward-analyzers-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Run static analyzers against fixture diff; assert every analyzer either passes or returns a typed finding with file:line.
