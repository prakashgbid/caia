# Scaffold template for @chiefaia/errors
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_ERRORS
title: "Typical work shape — errors"
description: |
  Package: @chiefaia/errors
  Survey-line: Typed error hierarchy and serialisation

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: live
importers_seen: 2
file_paths:
  - packages/errors/src/<feature>.ts
  - packages/errors/tests/<feature>.test.ts
  - packages/errors/README.md
success_criteria:
  output_file: reports/caia/errors-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Throw + serialise every typed error class; assert toJSON/fromJSON round-trip is lossless.
