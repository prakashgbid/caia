# Scaffold template for @pokerzeno/integrity-check
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_INTEGRITY_CHECK
title: "Typical work shape — integrity-check"
description: |
  Package: @pokerzeno/integrity-check
  Survey-line: Link & Action Integrity checker for Next.js sites. Guarantees zero broken links and zero dead click handlers.

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/integrity-check/src/<feature>.ts
  - packages/integrity-check/tests/<feature>.test.ts
  - packages/integrity-check/README.md
success_criteria:
  output_file: reports/stolution/integrity-check-<feature>.md
  min_bytes: 2000
  grep_match: "report|crawl|digest"
requires_merged_pr: true
phase_count: 3
deps: []
demonstrate_step: |
  Run crawler against fixture site; assert report contains expected URL count and no false-positive broken links.
