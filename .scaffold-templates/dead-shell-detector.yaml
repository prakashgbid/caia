# Scaffold template for @chiefaia/dead-shell-detector
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_DEAD_SHELL_DETECTOR
title: "Typical work shape — dead-shell-detector"
description: |
  Package: @chiefaia/dead-shell-detector
  Survey-line: 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: m3
state: dormant
importers_seen: 0
file_paths:
  - packages/dead-shell-detector/src/<feature>.ts
  - packages/dead-shell-detector/tests/<feature>.test.ts
  - packages/dead-shell-detector/README.md
success_criteria:
  output_file: reports/caia/dead-shell-detector-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 1
deps: []
demonstrate_step: |
  Scan caia/scripts/ for dead-shell candidates; assert report enumerates every candidate with last-modified + last-invoked.
