# Scaffold template for @chiefaia/stolution-dispatch
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_STOLUTION_DISPATCH
title: "Typical work shape — stolution-dispatch"
description: |
  Package: @chiefaia/stolution-dispatch
  Survey-line: MCP tool wrapper for spawning remote Claude Code workers on stolution via SSH

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/stolution-dispatch/src/<feature>.ts
  - packages/stolution-dispatch/tests/<feature>.test.ts
  - packages/stolution-dispatch/README.md
success_criteria:
  output_file: reports/stolution/stolution-dispatch-<feature>.md
  min_bytes: 1200
  grep_match: "pass|exit 0|vitest"
requires_merged_pr: true
phase_count: 2
deps: []
demonstrate_step: |
  Dispatch a remote claude worker via MCP tool; assert SSH spawn succeeds and worker exits 0.
