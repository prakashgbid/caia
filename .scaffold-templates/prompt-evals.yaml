# Scaffold template for @chiefaia/prompt-evals
# Generated by stolution-caia-package-survey (phase 1) on 2026-05-16.
# Feeds the v2-templated-scaffolder: a structured stub that lets the
# scaffolder spawn typical work for this package with zero LLM tokens.

id: PKG_PROMPT_EVALS
title: "Typical work shape — prompt-evals"
description: |
  Package: @chiefaia/prompt-evals
  Survey-line: Promptfoo-based eval suites for CAIA agent prompts. Wave 1.2 of the Enterprise Wave 1 campaign per agent/memory/enterprise_ai_landscape_directive.md (W1-2). Ships YAML test suites for the PO, BA, EA, 

  Typical work for this package is captured below. The scaffolder reads
  this template, substitutes feature-name + dep IDs, and emits a chain
  spec ready for dispatch. Override any field at scaffold time.
machine: stolution
state: dormant
importers_seen: 0
file_paths:
  - packages/prompt-evals/src/<feature>.ts
  - packages/prompt-evals/tests/<feature>.test.ts
  - packages/prompt-evals/README.md
success_criteria:
  output_file: reports/stolution/prompt-evals-<feature>.md
  min_bytes: 2000
  grep_match: "pass rate|baseline|canonical-suite"
requires_merged_pr: false
phase_count: 3
deps: []
demonstrate_step: |
  Run the full eval suite; assert pass rate >= baseline (no regression) and a per-prompt score table is written.
