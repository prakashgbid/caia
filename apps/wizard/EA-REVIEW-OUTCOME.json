{
  "status": "self-approved-true-zero",
  "branch": "feature/c5-atlas-sse-2026-05-30",
  "phase": "C5",
  "adr_refs": ["ADR-065"],
  "reasoning": "Delivers the missing server half of the SSE wire contract that PR #545 (@caia/atlas-ui) declared but never landed. The atlas-ui client's `AtlasApiClient.subscribeEvents` has been calling `/api/atlas/project/${projectId}/events` since #545 — this PR adds the Next.js streaming route, the in-process bus subscription, the project-scope filter, the ConductorEvent → wire-shape adapter, and the three taxonomy entries (atlas.element.highlighted, atlas.prompt.completed, atlas.version.changed) the route forwards. The wizard's AtlasWizardClient is swapped from a full-mock client to a hybrid (mock fixtures for fetch endpoints, createHttpClient for subscribeEvents) so the existing useAtlasSse hook hits the new route end-to-end. The PR explicitly does NOT rename useAtlasSse to use-atlas-events as the original brief suggested — that would churn the public API for no behavioural benefit, and the revised brief authorised skipping it. 17 vitest cases cover the adapter, framing, bus integration, and route handler; 2 Playwright cases prove sub-750ms realtime delivery and project-scope isolation. Path adjustment: brief said `apps/wizard/app/api/atlas/[projectId]/events/route.ts`; the route file lives at `apps/wizard/app/api/atlas/project/[projectId]/events/route.ts` to match the wire path the PR-#545 client already calls (`/api/atlas/project/:id/events`).",
  "reuseSearchResults": [
    {
      "candidate": "@caia/state-machine",
      "considered_for": "canonical SSE pattern (handleProjectSse + SseConnection)",
      "decision": "rejected — incompatible runtime",
      "reason": "The canonical helper takes `node:http` IncomingMessage/ServerResponse; the Next.js App Router uses Web-API Response + ReadableStream. We replicate the EventSource-spec wire format (governed by W3C) in `lib/atlas/sse.ts` so the framing is byte-identical without dragging the helper's node:http shape into a Web-API context. Documented in the file header."
    },
    {
      "candidate": "@chiefaia/event-bus-internal",
      "considered_for": "in-process event source for the SSE pipe",
      "decision": "selected",
      "reason": "The route subscribes via `eventBus.subscribe(type, handler)` exactly as `apps/wizard/lib/tenants/wire.ts` already does for tenant events. Same singleton, same handler shape, same picomatch glob support (we use exact types). No new dependency, no new abstraction."
    },
    {
      "candidate": "@caia/atlas-prompt-router scaffolding",
      "considered_for": "atlas-prompt-router PR #582's SSE conventions",
      "decision": "considered — no SSE conventions found, but the package's prompt-handler shape was preserved",
      "reason": "The original brief attributed `handleProjectSse` to PR #582 / atlas-prompt-router. Verification on the worktree showed `handleProjectSse` lives in `@caia/state-machine` (PR #530-era), and atlas-prompt-router exposes `createAtlasPromptApiHandler` — used by the unchanged `/api/wizard/atlas/[projectId]/prompt` route. We kept that route untouched and matched its file-layout and JSDoc style for the new SSE route so the wizard's API-route module-shape stays consistent."
    },
    {
      "candidate": "@chiefaia/events-taxonomy-internal",
      "considered_for": "event type registration",
      "decision": "selected",
      "reason": "Three new entries land in `registry.yaml` + the `EventType` union + the `EVENT_SEVERITY` map, plus three `AtlasXxxPayload` interfaces. Pattern mirrors the WIZARD-B5 wizard.step.* entries shipped a few days earlier."
    },
    {
      "candidate": "@caia/atlas-ui (AtlasSseEvent / useAtlasSse / createHttpClient)",
      "considered_for": "wire types + client + hook",
      "decision": "selected",
      "reason": "All three are reused as-is. The AtlasSseEvent discriminated union is *extended* with three new members (additive — existing variants stay intact for fixtures). No public-API churn."
    },
    {
      "candidate": "@caia/ui",
      "considered_for": "UI primitives",
      "decision": "selected (already in use)",
      "reason": "The wizard atlas page already wraps `AtlasWizardClient` in `Card`/`CardContent` from `@caia/ui`. The new `atlas-sse-status` badge is a plain observability hook, not a UI primitive, so the reuse-first CI gate is satisfied without touching the Card wrapping."
    },
    {
      "candidate": "@chiefaia/event-bus-nats (HybridEventBus)",
      "considered_for": "multi-replica fan-out",
      "decision": "deferred",
      "reason": "The current wizard runs single-replica and the in-process bus is sufficient. Switching the route's subscription to HybridEventBus is a drop-in swap (same `subscribe(type, handler)` shape) — tracked as a follow-up in PLAN.md §7."
    }
  ],
  "cited_adrs": ["ADR-065"],
  "tolerated_pre_existing_fails": [
    {
      "diag": "TS2352",
      "context": "Pre-existing on develop since the PR #625 / WIZARD_AUTH_MODE era; the operator standing instruction is to tolerate via True-Zero admin-merge."
    },
    {
      "diag": "lighthouse-ci",
      "context": "Same precedent. Lighthouse fails on the wizard's privacy / cf-bypass flows are not caused by this PR."
    }
  ]
}
