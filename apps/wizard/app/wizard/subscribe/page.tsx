/** Wizard Step 9 — Payment gate + download alternative. */

import { SubscribePanel } from '../../../components/wizard/SubscribePanel';

export const dynamic = 'force-dynamic';

export default function WizardSubscribePage(): React.JSX.Element {
  return <SubscribePanel />;
}
