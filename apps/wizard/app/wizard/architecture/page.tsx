/**
 * Wizard Stage 4 — Information Architecture (real).
 */

import { ArchitecturePanel } from '../../../components/wizard/ArchitecturePanel';

export const dynamic = 'force-dynamic';

export default function ArchitecturePage(): React.JSX.Element {
  return <ArchitecturePanel />;
}
