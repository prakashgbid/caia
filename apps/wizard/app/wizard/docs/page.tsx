/**
 * /wizard/docs — index of all generated docs for the active project.
 */

import { DocsIndex } from '../../../components/wizard/DocsIndex';

export const dynamic = 'force-dynamic';

export default function DocsIndexPage(): React.JSX.Element {
  return <DocsIndex />;
}
