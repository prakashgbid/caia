/**
 * DASH-002 stub — re-exports the implementation from the
 * legacy URL location (task-runs/[session_id]). The legacy URL is redirected
 * here via next.config.js.
 */
export { default } from '../../../task-runs/[session_id]/page';
