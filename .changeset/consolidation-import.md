---
---

Consolidation: imported `prakashgbid/conductor`, `prakashgbid/framework`, `prakashgbid/pokerzeno-framework`, `prakashgbid/pokerzeno-plugins`, `prakashgbid/pokerzeno-site-template`, `prakashgbid/site-template`, and `prakashgbid/conductor-state` into the CAIA monorepo. New `apps/*` dirs added (executor, completeness-sentinel, orchestrator, dashboard, etc.). Internal-only sub-packages added under `packages/*-internal` (private).
