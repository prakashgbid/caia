{
  "name": "@caia/contracts",
  "version": "0.0.1",
  "description": "Single source of truth for all CAIA cross-service contracts (JSON-Schema + generated TS types).",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "schemas"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'lint: TODO'",
    "test": "echo 'test: TODO'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
