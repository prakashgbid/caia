import { TaskAllocator } from '../src';

describe('TaskAllocator', () => {
  it('should be instantiable', () => {
    const instance = new TaskAllocator();
    expect(instance).toBeDefined();
  });
  
  // Add more tests here
});