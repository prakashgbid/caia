import { PrManager } from '../src';

describe('PrManager', () => {
  it('should be instantiable', () => {
    const instance = new PrManager();
    expect(instance).toBeDefined();
  });
  
  // Add more tests here
});