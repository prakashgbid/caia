import { CommitOrchestrator } from '../src';

describe('CommitOrchestrator', () => {
  it('should be instantiable', () => {
    const instance = new CommitOrchestrator();
    expect(instance).toBeDefined();
  });
  
  // Add more tests here
});