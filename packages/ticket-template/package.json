{
  "name": "@chiefaia/ticket-template",
  "version": "0.2.0",
  "private": true,
  "description": "Zod-validated structured ticket schema defining the interface between planning agents and the execution pipeline",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}