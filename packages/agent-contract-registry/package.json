{
  "name": "@chiefaia/agent-contract-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Declarative registry of per-agent prompt contracts with a composable template builder. Enables dynamic rubric generation at runtime without hard-coded prompt strings.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "bench": "vitest bench --run"
  },
  "dependencies": {
    "@chiefaia/ticket-template": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "zod": "^3.23.8"
  }
}
