#!/usr/bin/env sh

# Conventional Commits enforcement (REM-010 — restored 2026-04-28).
# Skip the hook in CI to avoid an extra install pass; CI does its own checks.
[ -n "$CI" ] && exit 0

npx --no -- commitlint --edit "$1"
